name: SONAR CI

on:
  push:
  pull_request:
jobs: 
  SONAR: 
    uses: haidarim/ci-templates/.github/workflows/java-sonar-ci.yml@v1.1.9
    with:
      java-version: "21"
      project-key: "haidarim_jwt-auth"
      organization: "haidarim"
    secrets: 
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      

