name: SONAR CI

on:
  push:
  pull_request:
jobs: 
  release: 
    uses: haidarim/ci-templates/.github/workflows/java-sonar-ci.yml@v1.1.8
    with:
      java-version: "21"
      project-key: "jwt-auth"
      organization: "orelease"
    secrets: 
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      

